import React from 'react'
// import spinner from '../../img/spinner.gif'

const Spinner = () => {
    return (
        // <img src={spinner} style={{width: '200px', margin: 'auto', display: 'block',}} alt="Loading"/>
        <h1 style={{width: '200px', margin: 'auto'}}>Loading...</h1>
    )
}

export default Spinner